# Normalize text files to LF in the repository
* text=auto eol=lf

# Windows PowerShell scripts use CRLF
*.ps1 text eol=crlf

# Binary assets — never apply EOL conversion
*.ico  binary
*.syso binary
*.exe  binary
*.png  binary
